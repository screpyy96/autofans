// A distinct route module gives each conversation a stable, shareable URL
// while keeping the inbox and thread implementation in one place.
export { action, default, loader, meta } from './messages';
